{
  "LABEL": {
    "name": "title needs adjustment",
    "color": "EEEEEE"
  },
  "CHECKS": {
  "regexp": "^(CD|CM)\-[0-9]{0,5}\:"
  },
  "MESSAGES": {
    "success": "PR title is valid",
    "failure": "PR title is invalid",
    "notice": "Title needs to pass regex '^(CD|CM)\-[0-9]{0,5}\:'"
  }
}
